const meta = {
  title: 'Sandbox',
  description: 'A development sandbox for full-stack realtime Javascript apps'
}

const titleTemplate = title => `${title} - ${meta.title}`

module.exports = {
  meta,
  titleTemplate
}
